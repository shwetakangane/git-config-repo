# welcome:
#    message: welcome test environment changed in git shweta
   spring:
  config:
    activate:
      on-profile: test
  datasource:
    username: sa
    password:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  jpa: 
    database-platform: org.hibernate.dialect.H2Dialect
  h2: 
    console:
      enabled: true
